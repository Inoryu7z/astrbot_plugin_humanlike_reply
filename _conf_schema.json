{
 "enable": {
  "description": "启用拟人回复",
  "type": "bool",
  "default": true,
  "hint": "关闭后插件将不拦截消息，AI 恢复秒回行为。可改用 /拟真开 /拟真关 命令按会话临时控制。",
  "obvious_hint": true
 },
 "judge_provider_id": {
  "description": "判断LLM的Provider ID",
  "type": "string",
  "default": "",
  "hint": "留空则使用当前会话的对话LLM。填写一个轻量模型的Provider ID可省token、加快判断速度。判断LLM和对话LLM的提示词主体都是人格提示词，只是追加的任务说明不同。"
 },
 "enable_private_chat": {
  "description": "启用私聊",
  "type": "bool",
  "default": true,
  "hint": "是否在私聊中启用拟人回复。"
 },
 "enable_group_chat": {
  "description": "启用群聊",
  "type": "bool",
  "default": false,
  "hint": "是否在群聊中启用拟人回复（按会话隔离）。若安装了 chat_plus 会自动让位群聊。"
 },
 "max_delay_minutes": {
  "description": "最大延迟分钟数",
  "type": "int",
  "default": 30,
  "hint": "判断LLM可设定的单次延迟上限（分钟）。防止LLM给出过长的延迟。"
 },
 "no_reply_cooldown_minutes": {
  "description": "不回复冷却(分钟)",
  "type": "float",
  "default": 5.0,
  "hint": "判断LLM选择「不回复」后，该会话在此期间不再触发判断（避免连续判断浪费token）。"
 },
 "enable_token_router_integration": {
  "description": "启用token_router联动",
  "type": "bool",
  "default": true,
  "hint": "开启后：手动触发token_router的on_message与on_llm_response钩子，让路由链与用量统计对本插件生效。token_router未安装时静默降级。",
  "obvious_hint": true
 }
}
