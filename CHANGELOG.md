### v1.0.4

**🔧 钩子链完整性修复**

**1. 🔧 立即回复路径重新接入框架完整消息发送流程**

* postsplitter 长消息分段生效。
* ttsplus 语音合成生效。
* thinkview 思考记录捕获生效。

**2. ⚠️ 已知限制**

* 延迟回复路径仍无法触发上述钩子（postsplitter / ttsplus / thinkview 在延迟回复时不生效）。

---

### v1.0.3

**🎛️ 会话级开关 + 配置精简**

**1. ✨ 新增会话级开关命令**

* `/拟真开`：按会话临时开启拟人回复。
* `/拟真关`：按会话临时关闭拟人回复。
* 不影响其他会话与全局 `enable`，优先级高于全局开关。

**2. 🧹 配置精简**

* 从 17 项缩到 7 项。
* 移除插件名类 / 超时 / 调试等不常调项，回退到代码默认值。

---

### v1.0.2

**🔗 TokenRouter 联动**

**1. 🔗 让 TokenRouter 的路由与用量统计对本插件生效**

* 本插件绕过框架流程直接调用 LLM，此前 TokenRouter 的路由和用量统计对本插件完全不生效。

**2. ⚙️ 新增配置项**

* `enable_token_router_integration`（默认开启）。
* `token_router_plugin_name`。

**3. 🛡️ 安全降级**

* TokenRouter 未安装时不影响主流程。

---

### v1.0.1

**🔗 ChatPlus 适配**

**1. 🔗 自动让位群聊**

* 检测到 chat_plus 已加载时，自动关闭本插件的群聊开关。
* 默认分工：chat_plus 处理群聊，本插件处理私聊。
* 避免双重决策与 API 浪费。

**2. ⚙️ 新增配置项**

* `chat_plus_plugin_name`。
* `auto_yield_group_chat_to_chat_plus`（默认开启，关闭可强制共存但不推荐）。

---

### v1.0.0

**🎭 首次发布：让 AI 像真人一样把控回复节奏**

**1. 🎭 拟人回复节奏**

* 用户发消息后，由判断 LLM 决定立即回复 / 延迟 X 分钟回复 / 不回复。
* 判断依据综合参考当前日程（由 DayFlow 注入）、用户消息内容与紧急程度。
* 判断 LLM 超时时兜底立即回复。
* 判断为"不回复"后进入冷却期（默认 5 分钟），避免连续判断浪费 token。

**2. 📝 二阶段审查**

* 判断 LLM 拟定回复方案草稿。
* 对话 LLM 审查方案：人设、事实、语气。
* 对话 LLM 可自由判断是否需要优化。

**3. ⏰ Dailysharing 冲突避免**

* 探测 daily_sharing 的下次主动分享时间。
* 延迟回复自动夹到不冲突范围（留 60 秒缓冲）。

**4. 🚨 催促重判**

* 用户在延迟期间发新消息时，取消原延迟任务并重新判断。
* 用户表示紧急时改为立即回复。
* 同一会话串行处理，避免重复回复或丢失消息。

**5. ⚙️ 完整配置项**

* 基础开关、LLM 相关、延迟与冷却、插件联动等。
